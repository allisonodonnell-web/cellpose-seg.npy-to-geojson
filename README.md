# cellpose-seg.npy-to-geojson
Convert Cellpose segmentation outputs (_seg.npy) to GeoJSON for QuPath

## Usage

Place your Cellpose `*_seg.npy` file in a folder with the script, then run:

```bash
python cellpose_seg_to_geojson.py sample_seg.npy

